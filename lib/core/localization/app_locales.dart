import 'package:flutter/material.dart';

class AppLocales {
  static const Locale enUS = Locale('en', 'US');
  static const Locale azAZ = Locale('az', 'AZ');

  static const List<Locale> supported = [enUS, azAZ];
}
